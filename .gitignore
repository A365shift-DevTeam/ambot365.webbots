# Node / Vite frontend (web/)
node_modules/
web/dist/
*.tsbuildinfo

# .NET API (api/)
.vs/
**/bin/
**/obj/
*.user
*.suo

# Uploaded images are user data, not source
api/Ambot365.Api/uploads/

# Both API settings files are committed on purpose: appsettings.json is the
# production configuration and appsettings.Development.json overrides it locally.
# Committing the development file is what stops a fresh clone from falling
# through to the production database.

# Preserved copy of the old Next.js JSON store
db/legacy-websites.json

# Misc
.DS_Store
*.pem
npm-debug.log*
.playwright-mcp/
